{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Run All Tests",
            "program": "zig",
            "args": ["build", "test", "--summary", "all"],
            "cwd": "${workspaceFolder}",
        },
    ]
}